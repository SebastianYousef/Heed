# Heed documentation

The [main README](../README.md) says what Heed is and how to install it. These pages are
for reading further — how each half works, what it can and cannot see, and why several
things are built the way they are rather than the obvious way.

| | |
|---|---|
| [How the filtering works](how-it-works.md) | Why a third-party app cannot stop a notification before it makes noise, what Heed does instead, the scoring pipeline, and how the model learns from you. |
| [Attention, limits and focus](attention.md) | Screen time, per-app rules, shared group budgets, breaking the feed, focus sessions, the grey screen, and how to read the charts. |
| [Feed detection](feed-detection.md) | How Heed tells a feed from a conversation, the anchors it ships, teaching it a new screen, and why a scroll count may only ever block in Automatic mode. |
| [Privacy](privacy.md) | Why the app has no network permission, what the screen access can and cannot see, what is stored and for how long, and the three export levels. |
| [Design decisions](decisions.md) | The choices that are not obvious from the code — including the banking workaround that was deleted, and what a full audit removed. |
| [Building and hacking](development.md) | Layout of the source, build and test commands, the one grant that needs a computer, signing, and what the app costs to run. |
| [Status](status.md) | What has been verified on a real device, what has not, and the known gaps in rough priority order. |

# Room writes its own implementations and Glance resolves receivers reflectively; both are
# covered by the libraries' own consumer rules. Nothing here yet — kept so that the day
# something does need keeping, the reason is written down beside it rather than copied in
# from a search result.
